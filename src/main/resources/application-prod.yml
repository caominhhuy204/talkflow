spring:
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false

springdoc:
  api-docs:
    enabled: false
  swagger-ui:
    enabled: false

app:
  auth:
    password-reset:
      expose-token: ${PASSWORD_RESET_EXPOSE_TOKEN:false}
